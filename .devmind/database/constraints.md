# Query Constraints & Limits

> AUTO-GENERATED by cohere. Last updated: 2026-03-13T12:54:46.467Z

## Query Limits

| Operation | Limit | Reason |
|-----------|-------|--------|
| SELECT rows | 10,000 max | Prevent context exhaustion |
| SELECT with OFFSET | 1,000 max | Performance degradation |
| JOIN depth | 5 max | Query complexity |
| INSERT rows (batch) | 1,000 max | Transaction size |
| Query timeout | 30s | Resource sharing |

## Required Patterns

### LIMIT Required
```sql
-- âœ“ CORRECT: Always use LIMIT
SELECT * FROM table LIMIT 100;
-- âœ— WRONG: No LIMIT on potential large tables
SELECT * FROM table;
```

## Pagination Patterns

### Keyset Pagination (Recommended)
```typescript
async function getNextPage(lastId: string, limit: number = 50) {
  return query(`SELECT * FROM items WHERE id > $1 ORDER BY id LIMIT $2`, [lastId, limit]);
}
```

## Context Guardrails

```typescript
const QUERY_CONSTRAINTS = {
  maxRows: 1000,
  maxDepth: 5,
  timeoutMs: 30000,
  requireOrderBy: true,
  requireLimit: true,
};
```

