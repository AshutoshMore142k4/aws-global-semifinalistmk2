# Database Context for AI Assistants

> AUTO-GENERATED by cohere. Core content works for all agents.

## Quick Reference

| Table | Primary Key | Foreign Keys | Sample Columns |
|-------|-------------|--------------|----------------|
**Relationship Summary:** No relationships defined

| Type | Description |
|------|-------------|
| 1:1 | One-to-one relationship |
| 1:N | One-to-many relationship |
| N:1 | Many-to-one relationship |
| N:M | Many-to-many relationship |

## Table Details


## Type Mappings

| Database Type | TypeScript Type | Notes |
|---------------|-----------------|-------|
| `uuid` | `string` (UUID v4) |
| `text` | `string`  |
| `varchar` | `string`  |
| `integer` | `number`  |
| `bigint` | `number`  |
| `boolean` | `boolean`  |
| `timestamp` | `Date`  |
| `jsonb` | `Record<string, unknown>`  |
| `int` | `number`  |
| `datetime` | `Date`  |
| `enum` | `string` (Enum values) |
| `INTEGER` | `number`  |
| `TEXT` | `string`  |
| `REAL` | `number`  |
| `BLOB` | `Buffer`  |
| `String` | `string`  |
| `Int` | `number`  |
| `Boolean` | `boolean`  |
| `DateTime` | `Date`  |

## Performance Tips

- Use indexes on frequently filtered columns (organization_id, deleted_at)
- Avoid SELECT *, specify needed columns for better performance
- Batch inserts with bulk operations when inserting multiple rows

## Platform Warnings

> [!WARNING|CODEX]
> **Codex**: Add to AGENTS.md:
> ```
> All queries MUST include organization_id filter.
> Pattern: SELECT ... WHERE id = $1 AND organization_id = $2
> ```

> [!WARNING|ANTIGRAVITY]
> **Antigravity**: Parallel agents share context. Use `.ai/memory/checkpoints/`.

> [!WARNING|ALL]
> **ALL AGENTS**: Checkpoint every 5-10 minutes to prevent context loss.
